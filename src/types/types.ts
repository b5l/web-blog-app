export type blogPostsType = {
  id: number;
  title: string;
  shortDescription: string;
};

export type blogDetailsType = {
  id: number;
  title: string;
  shortDescription: string;
  longDescription: string;
};
