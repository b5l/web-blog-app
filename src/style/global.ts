import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  pageContainer: {
    backgroundColor: "#CACACA",
    height: "100%",
  },
  button: {
    position: "absolute",
    right: 0,
    bottom: 0,
    margin: 30,
    borderRadius: 50,
  },
});
